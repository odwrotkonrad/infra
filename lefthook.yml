##[>] 🤖
extends:
  - ~/.config/lefthook/lefthook.yml

pre-commit:
  jobs:
    - name: docsgen
      run: make render-templates && git diff --exit-code
    - name: validate
      run: make validate
##[<] 🤖
